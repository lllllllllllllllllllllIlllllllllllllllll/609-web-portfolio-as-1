import React from 'react';
import {Flex} from '@chakra-ui/react';

export const Footer = () => {
    return(
        <Flex justifyContent={'center'}>
        
        <p>Powered by React & ChakraUI :)</p>
        </Flex>
    );
}